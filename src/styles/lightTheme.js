import theme from './theme';

export default {
  colors: {
    background: theme.colors.white,
    title: theme.colors.lightNavy,
    card: theme.colors.lightestSlate,
    projectText: theme.colors.darkGrey,
    icons: theme.colors.darkGrey,
    primaryText: theme.colors.navy,
    highLight: theme.colors.darkBlue,
    transColor: theme.colors.transBlue,
    footer: theme.colors.offWhite,
    footerText: theme.colors.lightNavy,
  },
};
