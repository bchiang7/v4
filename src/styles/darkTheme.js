import theme from './theme';

export default {
  colors: {
    background: theme.colors.navy,
    title: theme.colors.lightestSlate,
    card: theme.colors.lightNavy,
    projectText: theme.colors.lightSlate,
    icons: theme.colors.lightSlate,
    primaryText: theme.colors.slate,
    highLight: theme.colors.green,
    transColor: theme.colors.transGreen,
    footer: theme.colors.darkNavy,
    footerText: theme.colors.lightSlate,
  },
};
